// Crie um programa simples que mostre um alert pergunta: “Qual é a diferença entre if-else e switch?” e em seguida mostre um outro alert com a resposta.
alert(`Você sabe qual é a diferença entre if-else e switch?`);
alert(
  `A diferença é que usamos o If-else quando as comparações têm condições mais amplas e/ou complexas, diferente de um valor exatamente igual. Já o Switch deve ser utilizado quando as comparações forem idênticas ao parâmetro testado, com valores constantes.`
);
